# Plan: Maximizar calificación rúbrica
**Spec:** `_specs/maximizar-calificacion-rubrica.md`
**Fecha:** 2026-05-12
**Branch:** `opencode/feature/maximizar-calificacion-rubrica`

## Resumen de fases
| # | Fase | Agente | Archivos |
|---|------|--------|---------|
| 1 | Corrección de modelos y migración base | db-architect | 4 |
| 2 | Objetos PL/SQL (vistas, SP, función, triggers, tabla permisos) | db-architect | 1 |
| 3 | Seed data + usuario BD | db-architect | 2 |
| 4 | Backend: correcciones existentes | backend-developer | 8 |
| 5 | Backend: modelos de permisos + endpoints | backend-developer | 4 |
| 6 | Frontend: fixes | tanstack-wizard | 3 |
| 7 | Security review | security-code-reviewer | — |
| 8 | Verificación final | test-engineer | — |

---

## Fase 1 — Corrección de modelos y migración base
**Agente:** db-architect
**Objetivo:** Corregir columnas mal nombradas, agregar CHECK constraints, UNIQUE compuesto, índices compuesto y funcional

### Vehicle model — corregir columna
- Archivo: `taller-mecanico-api/app/models/vehiculo.py`
- Línea 17: renombrar `cli_date_mod` → `veh_date_mod`

### Order model — agregar CHECK + __table_args__
- Archivo: `taller-mecanico-api/app/models/orden.py`
- Agregar `__table_args__` con:
  - `CheckConstraint("ord_status IN ('ABIERTA','EN_PROCESO','CERRADA')", name="ck_orden_estado")`
  - `Index("idx_ordenes_status_fecha", "ord_status", "ord_date")`

### ServiceOrder model — agregar UNIQUE compuesto + CHECK + __table_args__
- Archivo: `taller-mecanico-api/app/models/orden_servicio.py`
- Agregar `__table_args__` con:
  - `UniqueConstraint("ord_id", "srv_id", name="uq_ord_servicio")`
  - `CheckConstraint("ords_hours > 0", name="ck_ords_horas_positivas")`

### Vehicle model — agregar CHECK año
- Archivo: `taller-mecanico-api/app/models/vehiculo.py`
- Agregar a `__table_args__`: `CheckConstraint("veh_year > 1900", name="ck_vehiculo_anio")`

### Modelo Service — agregar CHECK precio positivo
- Archivo: `taller-mecanico-api/app/models/servicio.py`
- Agregar `__table_args__`: `CheckConstraint("srv_price_hour > 0", name="ck_servicio_precio")`

### Nueva migración Alembic
- Archivo: `taller-mecanico-api/alembic/versions/xxxx_correcciones_modelo.py`
- `op.alter_column('Vehicles', 'cli_date_mod', new_column_name='veh_date_mod')`
- `op.create_index('idx_ordenes_status_fecha', 'Orders', ['ord_status', 'ord_date'])`
- `op.create_index('idx_vehiculo_upper_plate', 'Vehicles', [sa.text('UPPER(veh_plate)')])`
- Oracle-specific: índice funcional requiere sintaxis `op.execute("CREATE INDEX idx_vehiculo_upper_plate ON Vehicles (UPPER(veh_plate))")`
- Nota: CHECK y UNIQUE compuesto se generan desde SQLAlchemy `__table_args__` → no necesitan op.create explícito si se usa `alembic revision --autogenerate`

### Verificación
- [ ] `cd taller-mecanico-api && alembic upgrade head` ejecuta sin error
- [ ] `ruff check .` pasa sin errores

---

## Fase 2 — Objetos PL/SQL
**Agente:** db-architect
**Objetivo:** Crear todos los objetos de BD que no pasan por SQLAlchemy: tabla permisos_rol, funciones, SP, vistas, triggers, vista materializada. Usar migración Alembic con `op.execute()` para SQL raw.

### Archivo único
- `taller-mecanico-api/alembic/versions/xxxx_plsql_objects.py`

### Contenido de la migración (orden estricto):

#### 1. Tabla permisos_rol
```sql
CREATE TABLE permisos_rol (
    rol_id NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    rol_nombre VARCHAR2(50) NOT NULL UNIQUE,
    permiso_bitmask NUMBER(3) NOT NULL
);
```

#### 2. Seed de roles
```sql
INSERT INTO permisos_rol (rol_nombre, permiso_bitmask) VALUES ('admin', 255);     -- 0xFF
INSERT INTO permisos_rol (rol_nombre, permiso_bitmask) VALUES ('mecanico', 7);    -- 0x07
INSERT INTO permisos_rol (rol_nombre, permiso_bitmask) VALUES ('recepcion', 3);   -- 0x03
INSERT INTO permisos_rol (rol_nombre, permiso_bitmask) VALUES ('cliente', 1);     -- 0x01
```

#### 3. Función fn_tiene_permiso
```sql
CREATE OR REPLACE FUNCTION fn_tiene_permiso(
    p_rol IN VARCHAR2,
    p_permiso IN NUMBER
) RETURN NUMBER
IS
    v_mask NUMBER(3);
BEGIN
    SELECT permiso_bitmask INTO v_mask
    FROM permisos_rol
    WHERE rol_nombre = p_rol;
    IF BITAND(v_mask, p_permiso) = p_permiso THEN
        RETURN 1;
    ELSE
        RETURN 0;
    END IF;
EXCEPTION
    WHEN NO_DATA_FOUND THEN
        RETURN 0;
END fn_tiene_permiso;
```

#### 4. Función fn_calcular_total_orden
```sql
CREATE OR REPLACE FUNCTION fn_calcular_total_orden(
    p_ord_id IN NUMBER
) RETURN NUMBER
IS
    v_total NUMBER(10,2);
BEGIN
    SELECT COALESCE(SUM(ords_total), 0) INTO v_total
    FROM ServiceOrders
    WHERE ord_id = p_ord_id;
    RETURN v_total;
END fn_calcular_total_orden;
```

#### 5. Procedimiento sp_cerrar_orden
```sql
CREATE OR REPLACE PROCEDURE sp_cerrar_orden(
    p_ord_id IN NUMBER,
    p_mensaje OUT VARCHAR2
)
IS
    v_status VARCHAR2(50);
    v_pendientes NUMBER;
BEGIN
    SAVEPOINT sp_save;

    SELECT ord_status INTO v_status FROM Orders WHERE ord_id = p_ord_id;
    IF v_status = 'CERRADA' THEN
        p_mensaje := 'La orden ya está cerrada';
        RETURN;
    END IF;

    SELECT COUNT(*) INTO v_pendientes
    FROM ServiceOrders
    WHERE ord_id = p_ord_id AND ords_total IS NULL;

    IF v_pendientes > 0 THEN
        p_mensaje := 'Hay servicios sin total calculado';
        ROLLBACK TO sp_save;
        RETURN;
    END IF;

    UPDATE Orders SET ord_status = 'CERRADA' WHERE ord_id = p_ord_id;
    COMMIT;
    p_mensaje := 'Orden cerrada exitosamente';
EXCEPTION
    WHEN OTHERS THEN
        ROLLBACK TO sp_save;
        p_mensaje := SQLERRM;
        RAISE;
END sp_cerrar_orden;
```

#### 6. Vista compleja vw_resumen_clientes
```sql
CREATE OR REPLACE VIEW vw_resumen_clientes AS
SELECT c.cli_id, c.cli_name,
       (SELECT COUNT(*) FROM Vehicles v WHERE v.cli_id = c.cli_id) total_vehiculos,
       COUNT(o.ord_id) total_ordenes,
       SUM(CASE WHEN o.ord_status = 'CERRADA' THEN 1 ELSE 0 END) ordenes_cerradas
FROM Clients c
LEFT JOIN Orders o ON o.veh_id IN (SELECT veh_id FROM Vehicles v2 WHERE v2.cli_id = c.cli_id)
GROUP BY c.cli_id, c.cli_name;
```

#### 7. Vista simple vw_ordenes_activas
```sql
CREATE OR REPLACE VIEW vw_ordenes_activas AS
SELECT ord_id, veh_id, ord_status, ord_date, ord_urgency
FROM Orders
WHERE ord_status IN ('ABIERTA', 'EN_PROCESO');
```

#### 8. Vista materializada vm_metricas_taller
```sql
CREATE MATERIALIZED VIEW vm_metricas_taller
BUILD IMMEDIATE
REFRESH COMPLETE ON DEMAND
AS
SELECT TRUNC(ord_date) dia,
       COUNT(*) total_ordenes,
       SUM(CASE WHEN ord_status = 'CERRADA' THEN 1 ELSE 0 END) cerradas
FROM Orders
GROUP BY TRUNC(ord_date);
```

#### 9. Trigger trg_set_ses_id
Un trigger BEFORE INSERT por cada tabla que tenga ses_id. Ejemplo para Clients:
```sql
CREATE OR REPLACE TRIGGER trg_clients_ses_id
BEFORE INSERT ON Clients
FOR EACH ROW
BEGIN
    IF :NEW.ses_id IS NULL THEN
        :NEW.ses_id := SYS_CONTEXT('USERENV', 'SESSIONID');
    END IF;
END trg_clients_ses_id;
```
Repetir para: Vehicles, Orders, ServiceOrders, Service, users.

#### 10. Compound trigger trg_validar_cierre_orden
```sql
CREATE OR REPLACE TRIGGER trg_validar_cierre_orden
FOR UPDATE OF ord_status ON Orders
COMPOUND TRIGGER
    TYPE ord_id_tab IS TABLE OF Orders.ord_id%TYPE;
    v_ord_ids ord_id_tab := ord_id_tab();

    BEFORE EACH ROW IS
    BEGIN
        IF :NEW.ord_status = 'CERRADA' AND :OLD.ord_status != 'CERRADA' THEN
            v_ord_ids.EXTEND;
            v_ord_ids(v_ord_ids.LAST) := :NEW.ord_id;
        END IF;
    END BEFORE EACH ROW;

    AFTER STATEMENT IS
        v_pendientes NUMBER;
    BEGIN
        FOR i IN 1..v_ord_ids.COUNT LOOP
            SELECT COUNT(*) INTO v_pendientes
            FROM ServiceOrders
            WHERE ord_id = v_ord_ids(i) AND ords_total IS NULL;
            IF v_pendientes > 0 THEN
                RAISE_APPLICATION_ERROR(-20001,
                    'No se puede cerrar la orden ' || v_ord_ids(i) ||
                    ' porque tiene ' || v_pendientes || ' servicio(s) sin total');
            END IF;
        END LOOP;
    END AFTER STATEMENT;
END trg_validar_cierre_orden;
```

### Verificación
- [ ] `SELECT COUNT(*) FROM vw_resumen_clientes` retorna filas
- [ ] `SELECT fn_calcular_total_orden(1) FROM dual` retorna número
- [ ] `SELECT fn_tiene_permiso('admin', 8) FROM dual` retorna 1
- [ ] Compound trigger rechaza cierre de orden con servicios sin total

---

## Fase 3 — Seed data + usuario BD
**Agente:** db-architect
**Objetivo:** Poblar datos de prueba consistentes y crear script de usuario BD con privilegios mínimos

### Migración seed data
- Archivo: `taller-mecanico-api/alembic/versions/xxxx_seed_data.py`
- Migración raw SQL con INSERTs ordenados respetando FK:
  1. Sessions (3 filas: admin, mecanico, recepcion)
  2. Clients (3: Juan Pérez, María García, Carlos López)
  3. Vehicles (5: 2 de Juan, 2 de María, 1 de Carlos)
  4. Services (4: Cambio de Aceite, Frenos, Alineación, Diagnóstico)
  5. Orders (3: 1 abierta, 1 en proceso, 1 cerrada)
  6. ServiceOrders (~8: 2-3 por orden)
  7. Users (3: admin/admin123, mecanico/mec123, recepcion/rec123 — contraseñas hasheadas con bcrypt)
- Nota: `ses_id` se asigna por trigger, pasar NULL

### Script usuario BD
- Archivo: `taller-mecanico-api/db/create_taller_app_user.sql`
```sql
CREATE USER taller_app IDENTIFIED BY "TallerApp2026!";
GRANT CONNECT TO taller_app;
GRANT CREATE SESSION TO taller_app;
GRANT SELECT, INSERT, UPDATE, DELETE ON Clients TO taller_app;
GRANT SELECT, INSERT, UPDATE, DELETE ON Vehicles TO taller_app;
GRANT SELECT, INSERT, UPDATE, DELETE ON Orders TO taller_app;
GRANT SELECT, INSERT, UPDATE, DELETE ON ServiceOrders TO taller_app;
GRANT SELECT, INSERT, UPDATE, DELETE ON Service TO taller_app;
GRANT SELECT, INSERT, UPDATE, DELETE ON Sessions TO taller_app;
GRANT SELECT, INSERT, UPDATE, DELETE ON users TO taller_app;
GRANT SELECT, INSERT, UPDATE, DELETE ON permisos_rol TO taller_app;
GRANT EXECUTE ON fn_tiene_permiso TO taller_app;
GRANT EXECUTE ON fn_calcular_total_orden TO taller_app;
GRANT EXECUTE ON sp_cerrar_orden TO taller_app;
GRANT SELECT ON vw_resumen_clientes TO taller_app;
GRANT SELECT ON vw_ordenes_activas TO taller_app;
GRANT SELECT ON vm_metricas_taller TO taller_app;
ALTER USER taller_app QUOTA 100M ON USERS;
```

### Verificación
- [ ] Seed data inserta sin violar FK
- [ ] `SELECT * FROM permisos_rol` muestra 4 roles con bitmask correctos
- [ ] Script de usuario se ejecuta sin error en SQL*Plus o similar

---

## Fase 4 — Backend: correcciones existentes
**Agente:** backend-developer
**Objetivo:** Bugs identificados en lessons-learned.md + auth en todos los endpoints + SAVEPOINT

### dependencies.py — eliminar duplicación
- Archivo: `taller-mecanico-api/app/dependencies.py`
- Eliminar líneas 20-25 (primera definición de `require_permiso` que usaba `tiene_permiso` sin BD)
- Mantener solo la segunda definición (líneas 29-34) que usa `tiene_permiso_bd` con `db` param

### crud/vehiculo.py — renombrar clase
- Archivo: `taller-mecanico-api/app/crud/vehiculo.py`
- Cambiar `class CRUDOrden(CRUDBase[Vehicle]):` → `class CRUDVehicle(CRUDBase[Vehicle]):`
- Cambiar `crud_vehiculo = CRUDOrden(Vehicle)` → `crud_vehiculo = CRUDVehicle(Vehicle)`
- Verificar import en `app/routers/vehiculos.py` no se rompe (importa `crud_vehiculo`, no la clase)

### auth/jwt.py — mover SECRET_KEY a settings
- Archivo: `taller-mecanico-api/app/auth/jwt.py`
- Línea 6: eliminar `SECRET_KEY = "tu_clave_secreta_muy_larga_cambiala"`
- Reemplazar con `from app.core.config import settings` y usar `settings.SECRET_KEY` en `create_access_token` y `decode_token`
- Asegurar que `.env` tiene `SECRET_KEY` configurada

### Endpoints — agregar auth
- Archivos: `app/routers/clientes.py`, `vehiculos.py`, `ordenes.py`, `servicios.py`
- Import: `from app.dependencies import get_current_user`
- Agregar `current_user: User = Depends(get_current_user)` como dependencia en cada endpoint
- No proteger: `auth.py` (login/register), `root` en `main.py`

### SAVEPOINT en crud/orden.py
- Archivo: `taller-mecanico-api/app/crud/orden.py`
- En `create_with_services`: después de `try:`, agregar `db.begin_nested()` antes de crear la orden
- Si falla, rollback solo del savepoint (no de toda la transacción padre)

### Verificación
- [ ] `ruff check .` pasa
- [ ] Endpoint sin token retorna 401
- [ ] Endpoint con token válido funciona

---

## Fase 5 — Backend: modelos de permisos + endpoints
**Agente:** backend-developer
**Objetivo:** Modelo SQLAlchemy para permisos_rol, CRUD reader y endpoints para consultar roles desde frontend

### Modelo PermisoRol
- Archivo: `taller-mecanico-api/app/models/permiso_rol.py`
```python
from sqlalchemy import Column, Integer, String
from app.core.db import Base

class PermisoRol(Base):
    __tablename__ = "permisos_rol"
    rol_id = Column(Integer, primary_key=True)
    rol_nombre = Column(String(50), unique=True, nullable=False)
    permiso_bitmask = Column(Integer, nullable=False)
```

### Actualizar __init__.py
- Archivo: `taller-mecanico-api/app/models/__init__.py`
- Agregar: `from app.models.permiso_rol import PermisoRol`

### Endpoints de roles
- Archivo: `taller-mecanico-api/app/routers/auth.py`
- Agregar imports: `from app.models.permiso_rol import PermisoRol`, `from app.schemas.users import PermisoRolOut` (crear schema)
- `GET /api/v1/auth/roles` → `db.query(PermisoRol).all()`
- `GET /api/v1/auth/permisos/{rol}` → `db.query(PermisoRol).filter(PermisoRol.rol_nombre == rol).first()`
- No requieren auth (son públicos para login)

### Schema PermisoRolOut
- Archivo: `taller-mecanico-api/app/schemas/users.py`
```python
class PermisoRolOut(BaseModel):
    rol_nombre: str
    permiso_bitmask: int
    class Config:
        from_attributes = True
```

### Verificación
- [ ] `GET /api/v1/auth/roles` retorna JSON array de 4 roles con bitmask
- [ ] `ruff check .` pasa

---

## Fase 6 — Frontend: fixes
**Agente:** tanstack-wizard
**Objetivo:** Interceptor JWT en axios, tipos correctos, ruta vehiculos

### axios.ts — interceptor JWT
- Archivo: `auto-hub-pro/src/api/axios.ts`
- Agregar en el bloque `api.interceptors.request.use(...)`:
```ts
api.interceptors.request.use((config) => {
  const token = localStorage.getItem('token');
  if (token) {
    config.headers.Authorization = `Bearer ${token}`;
  }
  return config;
});
```
- Agregar interceptor response para 401:
```ts
api.interceptors.response.use(
  (response) => response,
  (error) => {
    if (error.response?.status === 401) {
      localStorage.removeItem('token');
      window.location.href = '/login';
    }
    return Promise.reject(error);
  }
);
```

### types/index.ts — corregir ServicioOrden
- Archivo: `auto-hub-pro/src/types/index.ts`
- Línea 17: cambiar `ords_id: number` → `srv_ord_id: number`

### vehiculos.tsx — corregir ruta delete
- Archivo: `auto-hub-pro/src/routes/vehiculos.tsx`
- Línea 118: cambiar ``api.delete(`vehiculos/${id}`)`` → ``api.delete(`/vehiculos/${id}`)``

### Verificación
- [ ] `npm run lint` pasa sin errores
- [ ] `tsc --noEmit` pasa sin errores

---

## Fase 7 — Security review
**Agente:** security-code-reviewer
**Objetivo:** Auditoría obligatoria de auth, ses_id, permisos, SAVEPOINT

### Checklist de revisión
- [ ] `dependencies.py` — solo una definición de `require_permiso`, usa `tiene_permiso_bd`
- [ ] `auth/jwt.py` — SECRET_KEY leída de settings (no hardcodeada)
- [ ] `app/routers/*.py` — todos los endpoints tienen `Depends(get_current_user)` excepto auth
- [ ] `crud/orden.py` — `begin_nested()` usado correctamente como SAVEPOINT
- [ ] `auth/permissions.py` — `ROLES` hardcodeado aún existe (redundante con tabla BD), no es crítico pero documentar
- [ ] `lessons-learned.md` — actualizar marcando bugs como resueltos

### Acción post-review
- Firmar aprobación en `lessons-learned.md`
- Si hay hallazgos, crear tareas de fix antes de continuar

---

## Fase 8 — Verificación final
**Agente:** test-engineer
**Objetivo:** Ejecutar toda la batería de verificación

| Comando | Paquete | Esperado |
|---|---|---|
| `ruff check .` | taller-mecanico-api | 0 errores |
| `npm run lint` | auto-hub-pro | 0 errores |
| `tsc --noEmit` | auto-hub-pro | 0 errores |

### Checklist completo
- [ ] `ruff check .` — 0 errores
- [ ] `npm run lint` — 0 errores
- [ ] `tsc --noEmit` — 0 errores
- [ ] Todas las migraciones Alembic ejecutan sin error
- [ ] Seed data insertada correctamente
- [ ] Vista materializada creada y consultable
- [ ] SP cierra orden con COMMIT/ROLLBACK + OUT
- [ ] Compound trigger rechaza cierre inválido
- [ ] Trigger asigna ses_id automáticamente
- [ ] Endpoints protegidos retornan 401 sin token
- [ ] Frontend inyecta JWT en cada request
- [ ] `graphify update .` ejecutado al final

---

## Resumen de archivos (17 únicos)
| # | Archivo | Acción | Fase |
|---|---------|--------|------|
| 1 | `taller-mecanico-api/app/models/vehiculo.py` | Modificar | 1 |
| 2 | `taller-mecanico-api/app/models/orden.py` | Modificar | 1 |
| 3 | `taller-mecanico-api/app/models/orden_servicio.py` | Modificar | 1 |
| 4 | `taller-mecanico-api/app/models/servicio.py` | Modificar | 1 |
| 5 | `taller-mecanico-api/alembic/versions/xxxx_correcciones_modelo.py` | Crear | 1 |
| 6 | `taller-mecanico-api/alembic/versions/xxxx_plsql_objects.py` | Crear | 2 |
| 7 | `taller-mecanico-api/alembic/versions/xxxx_seed_data.py` | Crear | 3 |
| 8 | `taller-mecanico-api/db/create_taller_app_user.sql` | Crear | 3 |
| 9 | `taller-mecanico-api/app/dependencies.py` | Modificar | 4 |
| 10 | `taller-mecanico-api/app/crud/vehiculo.py` | Modificar | 4 |
| 11 | `taller-mecanico-api/app/auth/jwt.py` | Modificar | 4 |
| 12 | `taller-mecanico-api/app/routers/{clientes,vehiculos,ordenes,servicios}.py` | Modificar | 4 |
| 13 | `taller-mecanico-api/app/crud/orden.py` | Modificar | 4 |
| 14 | `taller-mecanico-api/app/models/permiso_rol.py` | Crear | 5 |
| 15 | `taller-mecanico-api/app/models/__init__.py` | Modificar | 5 |
| 16 | `taller-mecanico-api/app/routers/auth.py` | Modificar | 5 |
| 17 | `taller-mecanico-api/app/schemas/users.py` | Modificar | 5 |
| 18 | `auto-hub-pro/src/api/axios.ts` | Modificar | 6 |
| 19 | `auto-hub-pro/src/types/index.ts` | Modificar | 6 |
| 20 | `auto-hub-pro/src/routes/vehiculos.tsx` | Modificar | 6 |
