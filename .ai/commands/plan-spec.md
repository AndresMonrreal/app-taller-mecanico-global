description: "Lee un spec de _specs/ y genera un plan de implementación detallado por fases en _plans/."

argument-hint: "Slug del spec (e.g. roles-permisos)"

steps:
  - name: "Step 1 — Resolver archivo de spec"
    description: >
      Extraer slug del argumento (quitar .md si está presente).
      Buscar _specs/<slug>.md. Si no existe, listar _specs/*.md y pedir al
      usuario que elija. Si no hay ninguno, imprimir error y detener.

  - name: "Step 2 — Leer todo el contexto"
    description: >
      Leer en paralelo:
      1. _specs/<slug>.md — el spec a planificar
      2. OPENCODE.md — convenciones del monorepo
      3. graphify-out/GRAPH_REPORT.md — estado actual del código
      4. .ai/persistence/lessons-learned.md — errores a evitar
      5. .ai/agents/db-architect.md
      6. .ai/agents/backend-developer.md
      7. .ai/agents/tanstack-wizard.md
      8. .ai/agents/security-code-reviewer.md
      No generar el plan hasta tener todo el contexto.

  - name: "Step 3 — Generar plan de implementación"
    description: >
      Actuar como Arquitecto Principal. Crear _plans/<slug>.md con fases
      ordenadas. Cada fase debe especificar: agente responsable, archivos
      exactos a crear/modificar, instrucciones Oracle-específicas, y
      criterios de verificación medibles.
    template: |
      # Plan: {{feature_name}}
      **Spec:** _specs/{{slug}}.md
      **Fecha:** {{current_date}}

      ## Resumen de fases
      | # | Fase | Agente | Archivos |
      |---|------|--------|---------|
      {{phases_summary}}

      ---

      ## Fase 1 — Base de datos
      **Agente:** db-architect
      **Objetivo:** {{db_objective}}

      ### Modelos SQLAlchemy
      - Archivo: `taller-mecanico-api/app/models/{{model_file}}.py`
      - Columnas Oracle: usar VARCHAR2, NUMBER, TIMESTAMP (nunca VARCHAR/FLOAT/DATETIME)
      - Identity: GENERATED BY DEFAULT AS IDENTITY
      {{model_details}}

      ### Migraciones Alembic
      - Archivo: `taller-mecanico-api/alembic/versions/{{migration_id}}_{{slug}}.py`
      {{migration_details}}

      ### PL/SQL (procedimientos, funciones, triggers, vistas)
      {{plsql_details}}

      ---

      ## Fase 2 — Backend
      **Agente:** backend-developer
      **Objetivo:** {{backend_objective}}

      ### Endpoints
      {{endpoints_detail}}

      ### Schemas Pydantic
      {{schemas_detail}}

      ### CRUD / Servicios
      {{crud_detail}}

      ### Permisos (bitmask)
      {{permissions_detail}}

      ---

      ## Fase 3 — Frontend
      **Agente:** tanstack-wizard
      **Objetivo:** {{frontend_objective}}

      ### Rutas TanStack
      {{routes_detail}}

      ### Componentes
      {{components_detail}}

      ### Hooks y API Client
      {{hooks_detail}}

      ---

      ## Fase 4 — Pruebas
      **Agente:** test-engineer
      {{tests_detail}}

      ---

      ## Verificación
      - [ ] ruff check . (taller-mecanico-api)
      - [ ] npm run lint (auto-hub-pro)
      - [ ] tsc --noEmit (auto-hub-pro)
      - [ ] Endpoints responden correctamente
      - [ ] UI renderiza sin errores en 1280x800 y 390x844
      {{extra_checks}}

  - name: "Step 4 — Confirmar"
    description: >
      Imprimir: ruta del plan generado y siguiente acción sugerida:
      `/execute-plan {{slug}}`