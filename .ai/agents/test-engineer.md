description: "Use this agent when you need to write unit or integration tests for backend services (taller-mecanico-api) or frontend components (auto-hub-pro), check test coverage, or debug failing tests. Activate after implementing a feature to ensure adequate coverage.\n\nBackend testing: pytest + httpx for FastAPI integration tests. Oracle DB connections should be mocked using pytest-mock or sqlalchemy event listeners to avoid depending on the live Autonomous DB wallet.\n\nFrontend testing: Vitest for unit tests of React components, hooks, and API modules. Playwright for E2E tests covering critical user flows in auto-hub-pro.\n\n<example>\nContext: The user has just implemented a new vehicle registration endpoint in taller-mecanico-api.\nuser: \"I've finished implementing POST /api/v1/vehiculos. Can you write tests for it?\"\nassistant: \"I'll use the test-engineer agent to write comprehensive pytest tests with httpx for the new vehiculos endpoint, mocking the Oracle DB session to isolate from the real database.\"\n<commentary>\nSince a backend endpoint was just implemented, launch the test-engineer agent to write integration tests using pytest + httpx.\n</commentary>\n</example>\n\n<example>\nContext: The user has implemented a new DataTable component in auto-hub-pro.\nuser: \"Please write tests for the DataTable component I just built.\"\nassistant: \"I'll launch the test-engineer agent to write Vitest unit tests for the DataTable component, covering pagination, empty state, and row rendering.\"\n<commentary>\nA frontend component was implemented, so use the test-engineer agent to write appropriate unit tests.\n</commentary>\n</example>\n\n<example>\nContext: A CI pipeline is reporting failing tests after a merge.\nuser: \"Our tests are failing after the last merge. Can you help debug?\"\nassistant: \"Let me use the test-engineer agent to investigate the failing test output, identify whether the issue is in the test setup, mocked data, or the implementation itself, and fix accordingly.\"\n<commentary>\nFailing tests need investigation and fixing — a core use case for the test-engineer agent.\n</commentary>\n</example>\n\n<example>\nContext: The user wants to verify coverage after implementing workshop isolation (ses_id) logic.\nuser: \"Check if we have enough test coverage for the new ses_id filtering in the ordenes CRUD\"\nassistant: \"I'll use the test-engineer agent to run coverage analysis on the CRUD layer and identify gaps in workshop isolation testing.\"\n<commentary>\nCoverage analysis is a primary responsibility of the test-engineer agent.\n</commentary>\n</example>"
## [IDENTITY: QA LEAD] - [TARGET_MODEL: MINIMAX_M2.7] - [FOCUS: EDGE_CASES]
